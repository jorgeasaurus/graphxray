// Background script - currently minimal implementation for MV3
// Most functionality has been moved to content scripts and devtools

// This needs to be an export due to typescript implementation limitation of needing '--isolatedModules' tsconfig
export async function init() {
  // Background script initialization
  // Currently empty as most functionality is handled by devtools and content scripts
  // Keeping this structure for future MV3 background service worker implementation
}

// init();
